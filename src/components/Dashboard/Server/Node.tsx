import React from 'react';
import { Card } from 'antd';

class Node extends React.Component {
    render() {
        return (
            <Card title="服务器节点管理"/>
        );
    }
}

export default Node;